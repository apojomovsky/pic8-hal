Vendored from: https://github.com/signal11/m-stack.git
Upstream commit: 979665546f3f39fb4b28a4acedfb1f9a5c55782e
Upstream commit date: 2017-09-28 16:47:19 -0400
Vendored on: 2026-07-15
License used: Apache License 2.0 (the Apache-2.0 arm of M-Stack's
LGPLv3/Apache-2.0 dual license -- see LICENSE-Apache2.txt in this
directory). Not relicensed.

Local fixes: this tree is NOT a pristine copy of the upstream commit above.
Three MPLAB XC8 v3.10 compatibility fixes are applied on top (a token-paste
placement macro, a min()/MIN() typo, a bit-field base type) -- see
pic8-usb/third_party/patches/0001-xc8-v3.10-compat.patch, which is this
exact diff, kept reapplicable for when this vendored copy is upgraded.
Details and why each was needed: pic8-usb/docs/pic8-usb-plan.md, "Phase 2
findings".

To upgrade to a newer upstream commit:
  1. git clone the URL above at the new commit.
  2. Replace this directory's contents with the fresh clone (keep this
     VENDORED_COMMIT file and pic8-usb/third_party/patches/, update the
     commit hash/date above).
  3. cd to the repo root and run:
       patch -p1 --dry-run < pic8-usb/third_party/patches/0001-xc8-v3.10-compat.patch
     If it applies cleanly, apply for real (drop --dry-run). If a hunk
     fails, upstream likely touched the same lines -- check whether
     upstream already fixed that issue (in which case drop that hunk) or
     re-derive the fix against the new code.
  4. Re-run the real compile validation (xc8-cc against usb.c, usb_cdc.c,
     this module's usb_descriptors.c and pic8_usb.c, targeting 18f4550 --
     see pic8-usb-plan.md's Phase 2 findings for the exact flags used) and
     the host test suite (ctest --test-dir pic8-usb/build) before trusting
     the upgrade.
